v1

# Memory summary

Navigational index. Full registry: `~/.cursor/memory/MEMORY.md`.

## Topics

- **preferences** — user workflow prefs (empty — populated by consolidation)
- **projects** — per-repo notes tagged by cwd (empty)
- **procedures** — reusable workflows stored in memory (empty)

## Lookup

Before re-specifying conventions: Grep `MEMORY.md` for task keywords → open max 1–2 linked files → stop after ~4–6 steps.
